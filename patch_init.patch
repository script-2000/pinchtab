--- internal/bridge/init.go
+++ internal/bridge/init.go
@@ -19,10 +19,13 @@
 }
 for _, name := range lockFiles {
 path := profileDir + "/" + name
-if err := os.Remove(path); err == nil {
+err := os.Remove(path)
+if err == nil {
 slog.Warn("removed stale chrome lock file", "path", path)
+} else if !os.IsNotExist(err) {
+slog.Warn("failed to remove chrome lock file", "path", path, "error", err)
 }
 }
 }
